{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": "scripts/npx.js",
  "scripts": {
    "build": "npm run workspaceify && turbo run build",
    "dev": "npm run workspaceify && turbo run dev --parallel",
    "lint": "npm run workspaceify && turbo run lint",
    "test": "npm run workspaceify && turbo run test",
    "workspaceify": "npm pkg set workspaces[]='apps/*' workspaces[]='packages/*'"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "latest",
    "@typescript-eslint/parser": "latest",
    "eslint": "^7.32.0",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
