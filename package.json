{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "ls": "node --eval \"console.log(JSON.stringify(Object.fromEntries(process.argv.slice(1).map(function(element) { element = element.substring(2); return [element, element + \\\"/**\\\"]; }))));\" -- $(find . -mindepth 2 -name \"package.json\" -not -path \"*/.*\" -not -path \"*/node_modules/**\" -exec dirname {} +)",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "latest",
    "@typescript-eslint/parser": "latest",
    "eslint": "^7.32.0",
    "turbo": "latest",
    "typescript": "latest"
  },
  "packageManager": "npm@8.5.5"
}
