{
	"name": "ci",
	"on": {
		"push": {
			"branches": ["main"]
		}
	},
	"jobs": {
		"enumerate": {
			"runs-on": "ubuntu-latest",
			"steps": [
				{ "uses": "actions/checkout@v3" },
				{
					"id": "enumerate",
					"run": "echo PACKAGES=\"[$(find . -name \"package.json\" -not -path \"*/.*\" -not -path \"*/node_modules/**\" | sed -z \"s/^/\\\"/;s/\\n$/\\\"/;s/\\n/\\\",\\\"/g\")]\" >> $GITHUB_OUTPUT"
				},
			],
			"outputs": {
				"packages": "${{ steps.enumerate.outputs.PACKAGES }}"
			}
		},
		"install": {
			"runs-on": "ubuntu-latest",
			"needs": ["enumerate"],
			"strategy": {
				"matrix": {
					"package": ["build", "lint", "test"]
				},
			},
			"steps": [
				{ "uses": "actions/checkout@v3" },
				{ "uses": "actions/setup-node@v3", "with": { "node-version": 18 } },
				{
					"uses": "bahmutov/npm-install@v1",
					"with": {
						"working-directory": "${{ join(fromJSON(needs.enumerate.outputs.packages), \"\n\") }}"
					}
				}
			]
		},
		"build": {
			"runs-on": "ubuntu-latest",
			"needs": ["install"],
			"strategy": {
				"matrix": {
					"script": ["build", "lint", "test"]
				},
			},
			"steps": [
				{ "uses": "actions/checkout@v3" },
				{ "uses": "actions/setup-node@v3", "with": { "node-version": 18 } },
				{ "uses": "bahmutov/npm-install@v1" },
				{ "run": "npm run ${{ matrix.script }}" }
			]
		},
		"changes": {
			"runs-on": "ubuntu-latest",
			"needs": ["build"],
			"steps": [
				{ "uses": "actions/checkout@v3" },
				{
					"id": "enumerate",
					"run": "echo PACKAGES=\"{\\\"apps/web\\\":\\\"apps/web/**\\\"}\" >> $GITHUB_OUTPUT"
				},
				{
					"uses": "dorny/paths-filter@v2",
					"id": "filter",
					"with": {
						"filters": "${{ steps.enumerate.outputs.PACKAGES }}"
					}
				}
			],
			"outputs": {
				"packages": "${{ steps.filter.outputs.changes }}"
			}
		},
		"release": {
			"if": "${{ needs.changes.outputs.packages != '' && needs.changes.outputs.packages != '[]' }}",
			"runs-on": "ubuntu-latest",
			"needs": ["changes"],
			"strategy": {
				"matrix": {
					"package": "${{ fromJSON(needs.changes.outputs.packages) }}"
				}
			},
			"steps": [
				{
					"run": "echo ${{ matrix.package }}"
				}
			],
			"outputs": {
				"package": "${{ matrix.package }}"
			}
		},
		"publish": {
			"if": false,
			"runs-on": "ubuntu-latest",
			"needs": ["release"],
			"steps": [
				{ "uses": "actions/checkout@v3" },
				{ "uses": "actions/setup-node@v3", "with": { "node-version": 18 } },
				{
					"uses": "bahmutov/npm-install@v1",
					"with": {
						"working-directory": "${{ needs.release.outputs.package }}"
					}
				},
				{ "run": "npm run build" },
				{
					"env": {
						"NODE_AUTH_TOKEN": "${{ secrets.NPM_TOKEN }}"
					},
					"run": "npx turbo run publish"
				}
			]
		}
	}
}
